WanderLust is a full-stack web application built using Node.js, Express, MongoDB, and EJS. It allows users to create, view, edit, and delete listings with proper authentication, validation, and error handling.

This project follows RESTful routing, uses JOI for server-side validation, Passport.js for authentication, and Flash messages for user feedback.